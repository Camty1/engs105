        PROGRAM test
        INTEGER, PARAMETER :: N = 5
        INTEGER, PARAMETER :: BANDWIDTH = 1
        REAL*8, DIMENSION(N, 2*BANDWIDTH+1) :: A
        REAL*8, DIMENSION(N) :: b
        INTEGER :: i, info

        
        END PROGRAM
